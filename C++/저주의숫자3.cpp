#include <iostream>
#include <stdio.h>
#include <string>
#include <vector>
#include <algorithm>

using namespace std;
/*
int solution(int n);

int main() {
  int answer = solution(40);

  cout << answer;
}

int solution(int n) {
  int answer = 0;

  for (int i =1; i < n + 1; ++i)
    {
      ++answer;
      while (true)
      {
          if (answer % 3 != 0 && to_string(answer).find("3") == string::npos)
              break;
         ++answer;
      }
    }
  return answer;
}*/